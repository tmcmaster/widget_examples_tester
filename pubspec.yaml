name: widget_examples_tester
description: A new Flutter package project.
version: 0.0.1
homepage: https://github.com/tmcmaster/widget_examples_tester/blob/main/README.md

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=2.2.0"

flutter:
  uses-material-design: true

dependencies:
  flutter:
    sdk: flutter
  dropdown_search: ^5.0.2
  flutter_riverpod:
  flutter_workbench:
    git:
      url: https://github.com/tmcmaster/flutter_workbench.git

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic:
  lint:
  test:
  faker:

dependency_overrides:
  flutter_workbench:
    path: ../..
